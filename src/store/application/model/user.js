export class User {

    constructor(
        id,
        created_at,
        name,
        email,
        password
    ) {
        this.id = id
        this.created_at = created_at
        this.name = name
        this.email = email
        this.password = password
    }

}